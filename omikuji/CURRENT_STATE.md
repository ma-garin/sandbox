# CURRENT_STATE.md — omikuji

> **更新タイミング**: 各セッション終了時に更新する。
> **次回開始**: 「CURRENT_STATE.mdを読んで再開して」の1行でコンテキスト復元。

---

## プロジェクト名

omikuji（おみくじ帳 — 引いたおみくじと参拝を記録して読み返す PWA）

## 最終更新

2026-08-10

## 現在のフェーズ

初版を実装し、UI/UX の検証と改善を繰り返している段階。GitHub Pages への公開はこれから。

## できていること

- 写真 38 枚から 33 件の記録を書き起こし（`app/data/omikuji.json`、おみくじ30・参拝1・その他2）
- 一覧（年の区切り・検索・吉凶と種別での絞り込み）／詳細（歌・運勢・判断・同番号の引き直し案内）／記録（おみくじ・参拝のみの切り替え、編集、削除）
- PWA（manifest・Service Worker・オフライン閲覧・ホーム画面へ追加）
- 配色は `my_forward` に合わせた blue-grey 系。アイコンは Material Symbols を同梱
- デプロイ用ワークフロー `.github/workflows/omikuji.yml`（データ形式と参照ファイルを確認してから公開）

## 検証で直したこと

fable の検証エージェントに UI/UX を見せ、指摘を受けて直す形で 3 巡した。

| 回 | 主な指摘 | 対応 |
|---|---|---|
| 1 | 「神社名の記載なし」が23枚に反復／時間の区切りがない／詳細に前後送りがない | 神社名を補い、年見出しと前後送りを追加 |
| 2 | 編集で種類を変えると本文が警告なく消える／同番号の引き直しに気づけない／検索が表示形式で引けない | 種類に関わらず入力を保持、同番号リンク、NFKC正規化とAND検索 |
| 3 | （実施中） | — |

## 次のタスク

- [ ] `main` に push して Pages を公開し、実機（スマホ）で「ホーム画面に追加」と一連の操作を確認する
- [ ] 検証エージェントのラウンドを続ける（上限10回のうち3回まで実施）
- [ ] `omikuji/app/images/` と `omikuji/app/thumbs/` をディスクから削除する（`.gitignore` 済みだがローカルに 9.4MB 残っている）

## 確かでないところ・弱いところ

- **日付の推定 2 件** — EXIF が除去された写真を IMG 連番から線形補間した。撮影ペースが一定という仮定に立っており、実際とずれている可能性がある
- **神社名の推定 21 件** — 恋みくじ形式が毎月1件ずつ揃うことと 2026-01-02 の記念カードを根拠にしたが、東京大神宮と直接書かれた券面は確認していない
- **本文を読み取れなかった箇所** — 2025-02-24（太宰府天満宮 第十三番）は運勢・判断がほぼ判読できず `confidence: "low"`。2026-03-01 の冒頭語も未判読
- **自動テストがない** — 確認は Playwright の手動操作のみ。回帰を検出する仕組みがない
- **写真の原本がリポジトリ外** — `data/` を失うと書き起こしの根拠を検証できなくなる。バックアップは利用者の手元のみ
