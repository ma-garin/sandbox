# Claude Code ガイドライン

このプロジェクトで Claude が従うべきルール。

## 禁止事項

### スケジューリング・自動実行の禁止
- `ScheduleWakeup` ツールを使用禁止
- `send_later` ツールを使用禁止
- `create_trigger` ツールを使用禁止
- `mcp__Claude_Code_Remote__create_trigger` ツールを使用禁止
- `mcp__Claude_Code_Remote__send_later` ツールを使用禁止
- Cron ジョブを勝手に設定しないこと
- 定期実行トリガーを勝手に設定しないこと

**例外**: ユーザーが明示的に「定期実行を設定してください」または「トリガーを作成してください」と指示した場合のみ、ユーザーの明示的な許可を得た上で実行できます。

### 理由
前回のセッションで意図しないトリガーが大量に設定され、1時間ごとに自動チェックが実行されていました。これを防ぐため、このルールを設定しました。

## その他のルール

- 定期的なチェックが不要な場合は、ユーザーに聞くまで何もしない
- 重要な決定や変更を加える前に、必ずユーザーに確認する
- 変更をコミットする前に、ユーザーに内容を説明する
