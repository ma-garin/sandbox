# CURRENT_STATE.md — docready-triage

> **更新タイミング**: 各セッション終了時に更新する。
> **次回開始**: 「CURRENT_STATE.mdを読んで再開して」の1行でコンテキスト復元。

---

## プロジェクト名

docready-triage

## 最終更新

2026-08-05

## 現在のフェーズ

引き継ぎ受領 — 本体ファイル待ち

## 直近の完了タスク

- `_template/` からフォルダを作成し、引き継ぎ内容を AGENTS.md / CURRENT_STATE.md に転記

## 次のタスク（優先順）

1. **リファクタ**: 1ファイルに同居している JS を「判定ロジック」「描画」「入出力」の
   3ブロックに整理し、判定ルールをデータ構造（`CHECKS` / `DEFAULT_FORM`）として分離する。
   ロジックの振る舞いは変えない
2. **判定ルールの外部定義化**: ルール定義を単一の JSON オブジェクトにまとめ `出典` フィールドを持たせる。
   「根拠」タブの表をその定義から自動生成し、二重管理を解消する
3. **複数登録の効率化**: CSV / タブ区切りテキストの貼り付けによる一括登録の入口を追加する
4. **索引出力のバリエーション**: 現在 Markdown のみ。JSON 形式（機械可読）の出力を追加する
5. **README.md の作成**: 何のツールか / なぜ Markdown 変換が危険か / 使い方 / 判定ルールの根拠一覧

## 未解決の判断待ち事項

- **`docready-triage.html`（PoC v0.1）の本体がリポジトリに未投入。**
  引き継ぎ書は「まず現状のコードを読んでから着手」を前提としているが、
  本リポジトリにも Git 履歴にもファイルが存在しない。
  タスク1（リファクタ）は現物なしでは着手できないため、
  `docready-triage/docready-triage.html` への配置待ち

## 既知の問題・技術的負債

- 判定ルールが「判定ロジック」と「根拠タブの出典表」に二重管理されている（タスク2で解消予定）
- 状態はメモリ保持のみ。リロードで消える（JSON の手動保存・読込で代替する設計上の割り切り）

## 重要な設計決定

- ブラウザストレージを使わない: 社内文書のメタ情報を端末に残さないため。永続化は JSON 入出力で代替
- 判定は「最適化」ではなく「足切り」: 情報が消える変換を避けることが目的
- 判定ルールは必ず出典を伴わせる: evidence-only

## セッション開始時の指示テンプレート

```
CURRENT_STATE.mdを読んで、次のタスクから作業を再開してください。
プロジェクト: /Users/fujimagariyuki/Desktop/app/sandbox/docready-triage/
```
