# AI2DocFrendry — 実行時依存
#
# 上限を必ず付ける（判断軸「再現性」／NFR-09 決定性／NFR-16 環境構築）。
# 下限は動作確認済みの最小版、上限は破壊的変更が入りうる次のメジャー（0.x系は次のマイナー）。
# 版数の根拠: PyPI の最新安定版を 2026-07-22 に取得して確認。
# 更新手順: 上限を上げる際は W-16 総合テストを再実行し、本コメントの日付を更新すること。

# --- 投入・抽出 ---
python-docx>=1.1,<2.0            # 最新安定版 1.2.0
openpyxl>=3.1,<4.0               # 最新安定版 3.1.5
python-pptx>=1.0,<2.0            # 最新安定版 1.0.2
pdfplumber>=0.11,<0.12           # 最新安定版 0.11.10（0.x系のためマイナーで上限）
beautifulsoup4>=4.12,<5.0        # 最新安定版 4.15.0
lxml>=5.2,<7.0                   # 最新安定版 6.1.1
charset-normalizer>=3.3,<4.0     # 最新安定版 3.4.9

# --- 再構成・出力 ---
markdown-it-py>=3.0,<5.0         # 最新安定版 4.2.0
PyYAML>=6.0,<7.0                 # 最新安定版 6.0.3

# --- UI ---
streamlit>=1.40,<2.0             # 最新安定版 1.60.0

# 要確認: 完全な再現性（NFR-09）には推移的依存も固定する必要がある。
#         クリーン環境で `pip freeze > requirements.lock.txt` を生成し、
#         W-16（総合テスト）時点の版で凍結すること。本ファイルは直接依存のみを規定する。
# 要確認: 層2（ローカルLLM）の実行系ライブラリは未確定（SPEC-OPEN）。確定後にここへ追記する。
