# CURRENT_STATE.md — DesignList

> **更新タイミング**: 各セッション終了時に更新する。
> **次回開始**: 「CURRENT_STATE.mdを読んで再開して」の1行でコンテキスト復元。

---

## プロジェクト名

DesignList

## 最終更新

2026-07-21

## 現在のフェーズ

実装中（Harness ToDo管理システムのモックを動作アプリ化）

## 直近の完了タスク

<!-- 完了したタスクを一言で -->
- `_template/` をコピーして DesignList フォルダを作成・初期化
- `harness-todo/gallery.html` にデザインモック集（ライト/ダーク/折りたたみ/状態）を保存
- `harness-todo/app/` にモックを落とし込んだ静的Webアプリを実装（5画面＋テーマ＋折りたたみ＋各状態）
- Chromium で全画面スクショ比較・JSエラーゼロを確認

## 次のタスク（最優先）

<!-- 次にやること（対象ファイルを明記） -->
- 未実装画面（カレンダー / プロジェクト）のデザイン・実装
- モックデータ（`app/js/data.js`）を実データ or API 連携へ差し替える設計

## 未解決の判断待ち事項

<!-- AIや自分が「どうする？」と保留にしていること -->
- 今後フレームワーク（React 等）へ移行するか、素のHTML/CSS/JSのまま拡張するか

## 既知の問題・技術的負債

<!-- 後で直す前提で放置しているもの -->
- カレンダー・プロジェクト画面はプレースホルダのみ
- データは静的モック（永続化なし。テーマ/折りたたみ/認証フラグのみ localStorage 保存）

## 重要な設計決定

<!-- なぜその選択をしたかを1行で -->
- 技術はビルド不要の素HTML/CSS/JS（GitHub Pages 直デプロイ・自己完結を優先）
- 状態集約＋全体 render() の単純構成（モック検証用途では見通しを優先）
- テーマはCSS変数のトークン切替のみ（モックの設計意図に合わせる）

## セッション開始時の指示テンプレート

```
CURRENT_STATE.mdを読んで、次のタスクから作業を再開してください。
プロジェクト: /Users/fujimagariyuki/Desktop/app/sandbox/DesignList/
```
