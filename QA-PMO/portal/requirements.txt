# ベリサーブ 品質ポータル — Python依存
# すべて無料のOSS（MIT/BSD/Apache/LGPL）。課金・登録なし。localhost完結。
# 起動: python manage.py runserver

# ── 基盤 ──
Django>=5.0,<5.2

# ── 提案B：UX刷新（HTMXによる部分更新）──
django-htmx>=1.20,<2.0

# ── 提案A：実績OSSへエンジン換装 ──
allpairspy>=2.5,<3.0      # ペアワイズ（MicrosoftのPICTと同系アルゴリズム）
pdfplumber>=0.11,<1.0     # 要件書PDF→テキスト抽出（ドキュメント検証の入力）
weasyprint>=60            # テスト計画書のPDF出力
markdown>=3.5,<4.0        # Markdown→HTML（PDF整形用）

# 注: 日本語ドキュメント校正は textlint（Node製OSS）を subprocess で利用。
#     導入は portal/textlint/ の package.json を参照（任意・未導入でも純Python版で動作）。
