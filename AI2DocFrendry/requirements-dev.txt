# AI2DocFrendry — 開発時依存（実行時には不要）
#
# 使い方: pip install -r requirements.txt -r requirements-dev.txt
# 版数の根拠: PyPI の最新安定版を 2026-07-22 に取得して確認。
# 設定は pyproject.toml に集約する。

-r requirements.txt

# --- テスト（NFR-15 カバレッジ80%以上） ---
pytest>=9.1,<10.0                # 最新安定版 9.1.1
pytest-cov>=7.1,<8.0             # 最新安定版 7.1.0

# --- 静的解析（NFR-12 認証情報非埋込 / NFR-14 行数・純粋関数） ---
ruff>=0.15,<0.16                 # 最新安定版 0.15.22（0.x系のためマイナーで上限）
mypy>=2.3,<3.0                   # 最新安定版 2.3.0
bandit>=1.9,<2.0                 # 最新安定版 1.9.4

# --- 依存の脆弱性検査 ---
pip-audit>=2.10,<3.0             # 最新安定版 2.10.1

# 要確認: CI（W-24）でどのツールをブロッキングにするかは未決定。
#         現時点では ruff / mypy / pytest をブロッキング候補、bandit / pip-audit を報告のみと想定。
